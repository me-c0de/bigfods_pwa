import {app} from './app';

export const path = {
  NOTIFICATION_ENDPOINT: app.API_ENDPOINT + 'notifications/',
  CAT_ENDPOINT: app.API_ENDPOINT + 'cats/',
  IMAGE_ENDPOINT: app.API_ENDPOINT + 'images/',
  USER_ENDPOINT: app.API_ENDPOINT + 'users/',
};
